name: Build

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  build:
    name: Build Linux packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install cargo-deb and cargo-rpm
        run: cargo install cargo-deb cargo-rpm

      - name: Build binary
        run: cargo build --release --locked

      - name: Build .deb package
        run: cargo deb

      - name: Build .rpm package
        run: cargo rpm build

      - name: Collect release artifacts
        run: |
          mkdir -p dist
          cp target/release/${{ github.event.repository.name }} dist/
          cp target/debian/*.deb dist/
          cp target/generate-rpm/*.rpm dist/ 2>/dev/null || cp target/rpm/x86_64/*.rpm dist/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-packages-${{ github.ref_name }}
          path: dist/
          if-no-files-found: error
